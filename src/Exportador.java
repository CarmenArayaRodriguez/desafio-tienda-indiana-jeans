import java.util.ArrayList;

public abstract class Exportador {
    // Método abstracto exportar que será implementado por las subclases
    public abstract void exportar(ArrayList<Producto> productos);
}
